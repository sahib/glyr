#ifndef P_LASTFM_H
#define P_LASTFM_H

#include "../core.h"
#include "../core.h"

const char * photos_lastfm_url(glyr_settings_t * settings);
cache_list * photos_lastfm_parse(cb_object * capo);

#endif
